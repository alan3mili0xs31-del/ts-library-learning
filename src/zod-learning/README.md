# Learning zod library
What is Zod?
It's a schema validator for typescript (with static type inference)
Why to use it?
Sometimes we don't know if data comming from external source is the expected
or has the structure we wish.
With Zod, we can assert that the comming data has the format and type we need
to avoid crashing our applications.
